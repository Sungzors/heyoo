<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_calendar_btn"
        android:icon="@drawable/ic_calendar_menu_shadow"
        app:elevation="4dp"
        android:title="@string/calendar"
        app:showAsAction="always"/>

    <group
        android:id="@+id/menu_group_calendar">
        <item
            android:id="@+id/action_full_cal"
            android:title="@string/month_view"
            app:showAsAction="never"/>

        <item
            android:id="@+id/action_partial_cal"
            android:title="@string/week_view"
            app:showAsAction="never"/>
    </group>

    <group
        android:id="@+id/menu_group_calendar2">
        <item
            android:id="@+id/action_new_event"
            android:title="@string/new_event"
            app:showAsAction="never"
            android:checkable="false"/>
        <item
            android:id="@+id/action_edit_calendar"
            android:title="Edit Calendar"
            android:titleCondensed="Edit"
            app:showAsAction="never"
            android:checkable="false"
            android:visible="false"/>
        <item
            android:id="@+id/action_view_members"
            android:title="Members ()"
            app:showAsAction="never"
            android:checkable="false"
            android:visible="false"/>
    </group>


</menu>